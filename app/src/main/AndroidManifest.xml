<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wxc.android.dagger.demo">

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modules.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.coffeemaker.CoffeeMakerActivity" />
        <activity android:name=".modules.beginner.BeginnerActivity" />
        <activity android:name=".modules.multibindings.MultibindingsActivity" />
        <activity android:name=".modules.subcomponent.SubcomponentActivity" />
        <activity android:name=".modules.android.way.AndroidActivity" />
        <activity android:name=".modules.android.way2.AndroidActivity2" />
    </application>

</manifest>